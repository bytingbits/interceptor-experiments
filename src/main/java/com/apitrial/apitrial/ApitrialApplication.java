package com.apitrial.apitrial;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication //this class gets created by the Spring Boot framework, by default, when you create a new Spring Boot project
public class ApitrialApplication {

	public static void main(String[] args) {
		SpringApplication.run(ApitrialApplication.class, args);
	}

}
